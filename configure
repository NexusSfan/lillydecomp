#!/usr/bin/env bash

# SPDX-License-Identifier: CC0-1.0

# Check for cl65
echo -n "Checking for cl65... "

CL65=$(command -v cl65)
if [ -z "$CL65" ]; then
    echo "not found"
    exit 1
else
    echo "$CL65"
fi

cp Makefile.in Makefile

# replace variables
sed -i "s|CL = |CL = $CL65|g" Makefile